source "https://rubygems.org"

# The theme's original Gemfile used `gemspec`, which breaks once
# minimal.gemspec is deleted. This mirrors what GitHub Pages runs.
gem "github-pages", group: :jekyll_plugins
gem "webrick"
